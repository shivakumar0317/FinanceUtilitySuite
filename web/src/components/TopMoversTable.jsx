import {Card,CardContent,Table,TableBody,TableCell,TableHead,TableRow,Typography} from "@mui/material";
export default function TopMoversTable({title,data}){return <Card><CardContent><Typography variant="h6" fontWeight={700}>{title}</Typography><Table size="small"><TableHead><TableRow><TableCell>Symbol</TableCell><TableCell align="right">Price</TableCell><TableCell align="right">Change %</TableCell></TableRow></TableHead><TableBody>{data.map(r=><TableRow key={r.symbol}><TableCell>{r.symbol}</TableCell><TableCell align="right">₹{Number(r.price).toLocaleString("en-IN")}</TableCell><TableCell align="right" sx={{color:r.change_percent>=0?"success.main":"error.main",fontWeight:700}}>{r.change_percent>=0?"+":""}{r.change_percent}%</TableCell></TableRow>)}</TableBody></Table></CardContent></Card>}
