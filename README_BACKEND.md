# Finance Utility Suite v1.5 Backend

1. `python -m pip install -r requirements-web.txt`
2. `Copy-Item .env.example .env`
3. `python run_backend.py`
4. Open `http://127.0.0.1:8000/docs`

SQLite works immediately. To use PostgreSQL, update `DATABASE_URL` in `.env`.
